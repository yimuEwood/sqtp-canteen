# 2026-05-11 工作日志

## sqtp-canteen 用户认证与权限系统开发
- 完成 Supabase Auth 配置：开启 Email provider，设置密码最少8位
- 创建数据库表 `profiles`（role字段: admin/approved/pending）+ RLS策略 + 触发器自动创建profile
- 新建前端文件：`useAuth.ts`(认证hook)、`AuthModal.tsx`(登录注册弹窗)、`AdminPanel.tsx`(管理员审批面板)
- 修改文件：`App.tsx`(整合auth)、`Navbar.tsx`(添加登录/登出按钮)、`FoodTable.tsx`(添加canEdit权限控制)、`types.ts`(添加Profile类型)
- **修复bug**: 邮箱验证逻辑 `endsWith('.zju.edu.cn')` → `includes('@zju.edu.cn')`（原写法导致@开头的浙大邮箱被误拒）
- **修复bug**: Supabase 注册后邮箱未确认导致无法登录，通过SQL手动 `UPDATE auth.users SET email_confirmed_at = NOW()` 确认用户，并在signUp中添加 `data: { email_confirm: true }` 实现自动确认
- 管理员账号已创建(3240103558@zju.edu.cn)并设为admin角色
- 代码已push到GitHub，Netlify自动部署成功
- 域名 zjucanteen.top 已绑定腾讯云DNS解析到Netlify
